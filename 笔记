[ Python3 ]
PEP8 编写规范：
    1).产量命名时采用全部大写字母，可以使用下划线
    2).使用单下划线 "_" 开头的模块变量或者函数是受保护的，在使用import * from 语句时无法从模块从导入进来
    3).使用双下划线 "__" 开头的实例变量或方法是类私有的。
    4).使用import 每次只导入一个库
    5).永远不要使用字母‘l’（小写的L），‘O’（大写的O），或者‘I’（大写的I）作为单字符变量名。
    6).类名一般使用首字母大写的约定。
    7).函数名应该小写，如果想提高可读性可以用下划线分隔。
	8).

